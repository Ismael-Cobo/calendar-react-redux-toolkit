export const eventsStyleGetter = (event, start, end, isSelected) => {
  const style = {
    backgroundColor: '#347CF7',
    borderRadius: 0,
    opacity: 0.8,
    border: 0,
    color: 'white',
  }

  return { style }
}
